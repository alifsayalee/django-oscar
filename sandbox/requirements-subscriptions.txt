# Extra requirements of the sandbox's subscription billing app (apps.subscriptions).
# Install after `pip install -e .[test]`:  pip install -r sandbox/requirements-subscriptions.txt
#
# The Maxio Advanced Billing SDK is not on PyPI; it is pinned to the commit this app was built
# and verified against. Its runtime dependencies are listed explicitly because an install from
# git can come out without them.
maxio-advanced-billing @ git+https://github.com/context-plugins/maxio-python-sdk.git@3d84ecefb9055ee3fba04e2b6cb22859aface190
httpx>=0.28.1,<1.0.0
pydantic[email]>=2.11.0,<3.0.0
typing-extensions>=4.13.0,<5.0.0
